import * as React from "react";

export const Tooltip = (props: any) => null;
export const TooltipTrigger = (props: any) => null;
export const TooltipContent = (props: any) => null;
export const TooltipProvider = (props: any) => null;
