import * as React from "react";

export const Accordion = (props: any) => null;
export const AccordionItem = (props: any) => null;
export const AccordionTrigger = (props: any) => null;
export const AccordionContent = (props: any) => null;
